# Where the API lives, for `npm run dev`.
#
# Copy to `.env.development` and edit. An empty value means "same origin", which is only
# useful if something in front of this app already forwards `/v1` -- there is no such
# proxy in this project, so leave it set to a real gateway.
VITE_API_BASE=http://127.0.0.1:8100
